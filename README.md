# 数学学習サイト

生徒向けに公開する教材（シミュレーションアプリ・プリント・参考動画）をまとめたサイトです。

## 構成

```
生徒向けサイト/
├── index.html         … トップページ（データを読み込んで一覧を自動表示）
├── apps/               … シミュレーションアプリ（HTML）
│   └── quadratic-inequality.html
├── data/
│   ├── prints.js       … プリント・教材の一覧データ
│   └── videos.js       … 参考動画の一覧データ
├── <単元名>/            … PDFなどの実ファイルを置くフォルダ（例: 2次関数/）
└── README.md           … このファイル
```

プリントと動画は、`index.html` を直接書き換えるのではなく **`data/prints.js` と `data/videos.js` にデータを1件追加するだけ** で一覧に反映される仕組みになっています（見た目は自動的に整います）。

## 教材を追加するときの流れ

### アプリ（HTML）を追加する場合
1. `apps/` フォルダにHTMLファイルを追加する
2. `index.html` の「シミュレーションアプリ」セクションにカードを1つ追加する

### プリント（PDF）を追加する場合
1. 単元名のフォルダ（例: `2次関数/`）にPDFを置く。フォルダがなければ新しく作ってよい
2. `data/prints.js` の配列に1件追加する（科目・単元・タイトル・ファイルパス）

### 動画を追加する場合
1. YouTubeのURL（例: `https://youtu.be/xxxxxxxxxxx`）から、末尾のID部分（`xxxxxxxxxxx`）を控える
2. `data/videos.js` の配列に1件追加する（科目・単元・タイトル・YouTube ID）
3. 他人の動画は埋め込み表示のみ。ファイルを保存してアップロードし直すのはNG（著作権）

**複数件まとめて追加したいときは、次の形式の表（テキストでもExcel/スプレッドシートでも可）でそのまま渡してもらえれば、データへの変換とpushはこちらで対応できます。**

プリント用の表の例：

| 科目 | 単元 | タイトル | ファイル名 |
|---|---|---|---|
| 数学Ⅰ | 2次関数 | 2次不等式 類題集 | 30_2次不等式_類題集.pdf |

動画用の表の例：

| 科目 | 単元 | タイトル | YouTube URL |
|---|---|---|---|
| 数学Ⅰ | 2次関数 | 2次不等式の解き方 | https://youtu.be/cLZZmxxSLFg |

### GitHubへの反映（共通）

```bash
git add .
git commit -m "add: 〇〇を追加"
git push
```

数分待つと公開サイトに反映されます。

## 公開URL

GitHub Pagesを有効化すると、以下のような形のURLになります（リポジトリ名は実際のものに置き換え）：

```
https://<GitHubユーザー名>.github.io/<リポジトリ名>/
```

## 注意事項

- **このフォルダには「生徒に見せてよいもの」だけを置く。** 個人情報を含む資料（名簿、指導要録、成績など）は絶対に入れない
- 他人が作った動画やアプリを直接コピーして置くのはNG。リンク（埋め込み）にする
- ファイル名はできるだけ半角英数字にする（日本語ファイル名だとURLが長くなり共有しづらいため）
