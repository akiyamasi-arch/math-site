# 先生の編集用ファイル（生徒向けサイトの公開コンテンツではないため、公開リポジトリには含めない）
*.xlsx
~$*.xlsx

# アプリの元ソース（apps/ にHTMLとして取り込み済みのため、公開サイトには含めない）
2次関数/*.txt

# 動画ファイル（サイトにはYouTubeの埋め込みで載せる。動画そのものは公開リポジトリに入れない）
*.mp4
*.mov
*.m4v
*.avi
*.wmv
